using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// If it's reloadable that means it has ammo, and mag, and rpm
public interface IReloadable
{
    //int CurrentAmmo { get; }
    //int TotalAmmoRemain { get; }
    //int MagSize { get; }
    //float Rpm { get; }

    //void Reload();
}
